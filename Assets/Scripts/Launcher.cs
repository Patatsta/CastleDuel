using UnityEngine;

public class Launcher : MonoBehaviour
{
    //[SerializeField] private AirMailPackage _boxPrefab;
    //[SerializeField] private Transform _shotPoint;
    //[SerializeField] private float _shotPower;

    //[SerializeField] private LabComplete _labComplete;
    //[SerializeField] private SimulatedPhysics _physics;
    //private void FixedUpdate()
    //{
    //    _physics.SimulatedTrajectory(_boxPrefab, _shotPoint.position, _shotPoint.forward * _shotPower);
    //}

    //private void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.Space))
    //    {
    //        var box = Instantiate(_boxPrefab, _shotPoint.position, Quaternion.identity);
    //        box.transform.parent = transform.root;
    //        box.Shot(_shotPoint.forward * _shotPower);
    //        _labComplete.CheckCriteria();
    //    }
    //}

}
